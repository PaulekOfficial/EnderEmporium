
import { Theme } from "@mui/system";

interface ShoppingCartOverlayProps {
    theme: Theme;
}

const ShoppingCartOverlay: React.FC<ShoppingCartOverlayProps> = ({ theme }) => {
    return (
        <>
        </>
    );
};

export default ShoppingCartOverlay;
